{
  "name": "whale-fetcher-server",
  "version": "1.0.0",
  "description": "Whale Wallet Discovery & Fetching API Server",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "build:render": "npm install && npm run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.10",
    "@types/node-schedule": "^2.1.7",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
